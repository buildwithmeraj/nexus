import React from "react";
import ClubsList from "../manager/ClubsList";

const ClubManager = () => {
  return (
    <div>
      <h2>Club Manager</h2>
      <ClubsList />
    </div>
  );
};

export default ClubManager;
